# Database configuration variables for docker-compose.yml
# Copy this file to .env in the same folder before running docker-compose up

# PostgreSQL service variables
POSTGRES_USER=leafsense_admin
POSTGRES_PASSWORD=leafsense_secure_pass_2026
POSTGRES_DB=leafsense_db
POSTGRES_PORT=5432

# pgAdmin administration tool variables
PGADMIN_DEFAULT_EMAIL=admin@leafsense.ai
PGADMIN_DEFAULT_PASSWORD=pgadmin_secure_pass_2026
PGADMIN_PORT=5050
